{% extends 'base.html.twig' %}

{% block title %}Edit Species{% endblock %}

{% block body %}
    <h1>Modifier l'espèce</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}

        <div class = "button_edit">    

            <button class="btn btn-edit">{{ button_label|default('Sauvegarder') }}</button>


    {{ form_end(form) }}

        {{ include('back/species/_delete_form.html.twig') }}

            <button class="btn btn-profil"><a href="{{ path('app_all_species') }}"> Retour à la liste</a></button>

        </div>

{% endblock %}
